# 🤝 Руководство по внесению изменений

Спасибо за интерес к улучшению этого проекта! Любой вклад приветствуется.

## 📋 Как я могу помочь?

### 🐛 Сообщить о баге

Нашли ошибку? Пожалуйста, создайте Issue на GitHub с описанием:

1. **Название**: Краткое описание проблемы
2. **Описание**: Подробное описание бага
3. **Шаги для воспроизведения**: 
   - Шаг 1
   - Шаг 2
   - ...
4. **Ожидаемое поведение**: Что должно было произойти
5. **Фактическое поведение**: Что произошло на самом деле
6. **Скриншоты**: Если применимо
7. **Окружение**:
   - Браузер и версия
   - ОС
   - Устройство (desktop/mobile)

### 💡 Предложить улучшение

Есть идея? Создайте Issue с тегом `enhancement`:

1. **Название**: Суть предложения
2. **Проблема**: Какую проблему решает
3. **Решение**: Как вы предлагаете это реализовать
4. **Альтернативы**: Другие варианты решения
5. **Примеры**: Mockup, ссылки на похожие реализации

### 🔧 Внести код

Хотите написать код? Следуйте инструкциям ниже.

---

## 🚀 Процесс разработки

### 1. Fork репозитория

Нажмите кнопку "Fork" на GitHub.

### 2. Клонируйте ваш fork

```bash
git clone https://github.com/YOUR_USERNAME/misha-warning.git
cd misha-warning
```

### 3. Создайте ветку для изменений

```bash
git checkout -b feature/your-feature-name
# или
git checkout -b fix/your-bug-fix
```

**Именование веток**:
- `feature/` - новая функциональность
- `fix/` - исправление бага
- `docs/` - изменения в документации
- `style/` - форматирование, CSS изменения
- `refactor/` - рефакторинг кода
- `test/` - добавление тестов

### 4. Внесите изменения

Редактируйте файлы, следуя [стандартам кода](#стандарты-кода).

### 5. Протестируйте локально

```bash
# Запустите локальный сервер
python3 -m http.server 8000

# Откройте http://localhost:8000
# Протестируйте изменения в разных браузерах
```

**Проверьте**:
- ✅ Все функции работают
- ✅ Адаптивность (mobile, tablet, desktop)
- ✅ Темная и светлая темы
- ✅ Нет ошибок в консоли браузера
- ✅ Код валидируется (HTML, CSS)

### 6. Commit изменений

```bash
git add .
git commit -m "Описание изменений"
```

**Формат commit-сообщений**:
```
тип: краткое описание (до 50 символов)

Подробное описание изменений (если нужно).
Объясните "что" и "почему", а не "как".

Связанные issues: #123, #456
```

**Типы**:
- `feat:` - новая функция
- `fix:` - исправление бага
- `docs:` - изменения в документации
- `style:` - форматирование, CSS
- `refactor:` - рефакторинг
- `test:` - добавление тестов
- `chore:` - обновление зависимостей и т.п.

**Примеры**:
```bash
git commit -m "feat: добавить кнопку экспорта данных"
git commit -m "fix: исправить переполнение текста на мобильных"
git commit -m "docs: обновить инструкции по деплою"
```

### 7. Push изменений

```bash
git push origin feature/your-feature-name
```

### 8. Создайте Pull Request

1. Перейдите в ваш fork на GitHub
2. Нажмите "New Pull Request"
3. Заполните шаблон:
   - **Название**: Краткое описание
   - **Описание**: Что изменено и почему
   - **Связанные issues**: #123
   - **Чеклист**: Отметьте выполненное
4. Нажмите "Create Pull Request"

---

## 📝 Стандарты кода

### HTML

```html
<!-- ✅ Хорошо -->
<section id="story" class="story-section">
    <div class="container">
        <h2>Заголовок</h2>
        <p>Текст параграфа.</p>
    </div>
</section>

<!-- ❌ Плохо -->
<div id="story">
    <div>
        <h2>Заголовок</h2>
        <p>Текст параграфа.</p>
    </div>
</div>
```

**Правила**:
- Используйте семантические теги (`<section>`, `<article>`, `<nav>`)
- Добавляйте `alt` к изображениям
- Используйте `aria-label` для кнопок без текста
- Индентация: 4 пробела
- Закрывайте все теги
- Атрибуты в двойных кавычках

### CSS

```css
/* ✅ Хорошо */
.story-section {
    padding: var(--spacing-xl) 0;
    background-color: var(--bg-secondary);
}

.story-section .container {
    max-width: 900px;
    margin: 0 auto;
}

/* ❌ Плохо */
.story-section{padding:80px 0;background:#1a1a1a;}
.story-section .container{max-width:900px;margin:0 auto}
```

**Правила**:
- Используйте CSS-переменные для цветов и размеров
- Mobile-first подход (базовые стили для мобильных, media queries для больших экранов)
- BEM-подобное именование классов
- Индентация: 4 пробела
- Пробелы после `:` и перед `{`
- Каждое свойство на новой строке

### JavaScript

```javascript
// ✅ Хорошо
const button = document.getElementById('theme-toggle');

button.addEventListener('click', () => {
    const body = document.body;
    body.classList.toggle('dark-mode');
    body.classList.toggle('light-mode');
    
    saveThemePreference();
});

function saveThemePreference() {
    const theme = body.classList.contains('light-mode') ? 'light' : 'dark';
    localStorage.setItem('theme', theme);
}

// ❌ Плохо
var button=document.getElementById('theme-toggle');
button.onclick=function(){
    document.body.classList.toggle('dark-mode');
    document.body.classList.toggle('light-mode');
    localStorage.setItem('theme',document.body.classList.contains('light-mode')?'light':'dark');
}
```

**Правила**:
- Используйте `const` и `let`, избегайте `var`
- Используйте стрелочные функции где возможно
- Именование: camelCase для переменных и функций
- Добавляйте комментарии для сложной логики
- Индентация: 4 пробела
- Точка с запятой обязательна

---

## 🎨 Дизайн

### Цвета

Используйте CSS-переменные из `css/style.css`:

```css
:root {
    --bg-primary: #0a0a0a;
    --bg-secondary: #1a1a1a;
    --bg-tertiary: #2a2a2a;
    --text-primary: #f1faee;
    --text-secondary: #a8b2b8;
    --accent-red: #e63946;
    --accent-red-dark: #dc2f02;
    --accent-red-light: #f77f00;
}
```

### Типографика

```css
--font-primary: 'Inter', sans-serif;
--font-size-base: 16px;
--font-size-small: 14px;
--font-size-large: 18px;
--line-height: 1.6;
```

### Spacing

```css
--spacing-xs: 0.5rem;  /* 8px */
--spacing-sm: 1rem;    /* 16px */
--spacing-md: 2rem;    /* 32px */
--spacing-lg: 3rem;    /* 48px */
--spacing-xl: 5rem;    /* 80px */
```

---

## 🧪 Тестирование

### Обязательные проверки

1. **Браузеры**:
   - Chrome (последняя версия)
   - Firefox (последняя версия)
   - Safari (если доступен)
   - Edge (если доступен)

2. **Устройства**:
   - Desktop (1920x1080, 1366x768)
   - Tablet (768x1024)
   - Mobile (375x667, 414x896)

3. **Темы**:
   - Темная тема
   - Светлая тема
   - Переключение между темами

4. **Функциональность**:
   - Все ссылки работают
   - Форма отправляется
   - Кнопки реагируют
   - Анимации плавные

### Инструменты для тестирования

- [W3C HTML Validator](https://validator.w3.org/)
- [W3C CSS Validator](https://jigsaw.w3.org/css-validator/)
- [PageSpeed Insights](https://pagespeed.web.dev/)
- [WAVE Accessibility Tool](https://wave.webaim.org/)

---

## 📚 Документация

При изменении кода обновляйте документацию:

- `README.md` - если меняется основная функциональность
- `CUSTOMIZATION.md` - если меняются инструкции по кастомизации
- `DEPLOYMENT.md` - если меняется процесс деплоя
- `CHANGELOG.md` - всегда добавляйте свои изменения

---

## ❓ Вопросы?

**Не стесняйтесь спрашивать!**

- Создайте Issue с вопросом
- Напишите в Discussions (если доступно)
- Проверьте существующие Issues - возможно, ответ уже есть

---

## 📜 Code of Conduct

### Наши принципы

- 🤝 Будьте дружелюбны и уважительны
- 🎯 Конструктивная критика приветствуется
- 🚫 Никакого harassment, дискриминации, оскорблений
- 📚 Помогайте новичкам

### Недопустимое поведение

- Троллинг, оскорбления, провокации
- Публикация личной информации без разрешения
- Спам и нерелевантный контент
- Любые формы harassment

**Нарушители будут заблокированы.**

---

## 🎉 Благодарности

Спасибо всем контрибьюторам проекта! Ваш вклад делает интернет безопаснее.

### Топ контрибьюторов

- [Ваше имя] - Создатель проекта

*(Список будет обновляться)*

---

## 📄 Лицензия

Внося изменения, вы соглашаетесь лицензировать свой вклад под [MIT License](LICENSE).

---

**Спасибо за помощь в защите людей от мошенников! 🛡️**
